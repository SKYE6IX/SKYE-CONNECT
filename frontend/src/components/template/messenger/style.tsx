'use client';
import styled from 'styled-components';

export const MessengerContainer = styled.div``;

export const MessengerHeader = styled.div``;

export const BackToFeedButton = styled.button`
  display: flex;
  align-items: center;
  font-size: 1rem;
  border: none;
  cursor: pointer;
  svg {
    font-size: 1rem;
  }
`;
